#!/usr/bin/env python
import rospy
import os
from std_msgs.msg import String

global status
status="map"
def callback(data):
    global status 
    print status 
    rospy.loginfo(rospy.get_caller_id() + "%s:", data.data)
    if data.data=="nav" and status=="map":
    	#os.system("ps -ef ")
	os.system("roslaunch  motion_planning  move_base.launch > /home/linaro/nav/src/startup/motion_planning.log 2>&1  &")
	#os.system("sudo initctl emit start_movebase  2>&1  &")
	#global status
        status="nav"
	
    
def listener():

    # In ROS, nodes are uniquely named. If two nodes with the same
    # node are launched, the previous one is kicked off. The
    # anonymous=True flag means that rospy will choose a unique
    # name for our 'listener' node so that multiple listeners can
    # run simultaneously.
    rospy.init_node('monitor', anonymous=True)

    rospy.Subscriber("switchmode", String, callback)

    # spin() simply keeps python from exiting until this node is stopped
    rospy.spin()

if __name__ == '__main__':
    listener()
